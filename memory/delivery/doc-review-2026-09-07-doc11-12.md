# Doc-review gate — DOC-11 + DOC-12 · [2026-09-07]

**Slice:** `04-platform` — DOC-11 v0.1 (Chốt, DEC-ARC-008) · DOC-12 v0.1 (Chốt, DEC-ARC-010) + `openapi.yaml`  
**Đối chiếu:** `hrm/backend` — `AppDbContext` 41 DbSet · 13 controller  
**Reviewer:** QC (một pass đối kháng) · **Verdict: ⛔ BLOCK baseline**

Pass này **xác nhận lại** review 2026-09-05: lệch chưa được xử lý. Bổ sung: **path contract** LEV/PRB/LIF không trùng chữ với DOC-12/`openapi.yaml`.

---

## Finding

### 🔴 Blocker

**[Blocker] Mâu thuẫn chéo — DOC-11 §1.2/§2/§3.5 · ADR-001**  
Bằng chứng: DOC-11 mô hình N DB (IAM vs EMP, PAY tách PII, cấm bảng lương trong IAM/LEV/TIM DB). Code: một `AppDbContext`, một `ConnectionStrings:AppDbContext`, một DB `hrm`.  
Vì sao lỗi: NFR-002 chỉ còn guard ứng dụng; §6 “replicate cả N DB” không áp dụng. Baseline = đóng băng kiến trúc chưa xây.  
Đề xuất: SA + PGD — CR DOC-11/ADR-001 về single-DB **hoặc** ghi nợ có deadline. Không baseline trước khi quyết.

**[Blocker] Đầy đủ + Mâu thuẫn — DOC-11 §3, §8**  
Bằng chứng: entity giấy không có trong code: `Payslip`, `PayFormulaSnapshot`, `PayrollPeriod`, `ProbationCase`, `ProbationTask`, `LmProposal`, `PrbDecision`, `LifecycleCase`, `AccessLock`, `ChecklistItem`, `AuthAudit`, `Contract`, `Education`, `TimesheetTemplate`, `ImportBatch`, `LeaveAudit`. Code có (không có trên giấy): `PayPeriod`/`PayLine`/`PayRegulation`/`Pay*Allowance*`/`PayExportOutbox`, `EmployeeContract`/`EducationLevel`/`SeniorityRule`/`LineManagerChangeRequest`/`EmpAuditLog`, `LeaveNotification`, `TimesheetTemplateVersion`/`Column`/`TimesheetImportBatch`/`Row`, `ProbationEvaluation`/`Outcome`/`Criterion`/`Reminder`/`ExtendDuration`, `LifOn/Off*`, `LifAccessLockOutbox`.  
Vì sao lỗi: §8 `NFR-002 → Payslip chỉ DB-PAY` trỏ entity không tồn tại (payslip = query `PayLine`).  
Đề xuất: SA reverse-engineer §3 từ snapshot EF; giữ §4 master data.

**[Blocker] Đầy đủ — DOC-12 §4 · openapi.yaml**  
Bằng chứng: §4 = 17 dòng; YAML = 16 path. Host ≈ 87 action / 13 controller. Ngoài spec: `DevAuth`, `EmpCatalog`, `LineManagerChangeRequests`, `TimekeepingDevice`, hầu hết TIM/PAY/LIF/PRB.  
Vì sao lỗi: baseline OAS = hợp đồng ~20% bề mặt.  
Đề xuất: SA export Swagger runtime → thay `openapi.yaml`.

**[Blocker] Mâu thuẫn — DOC-12 §4 / openapi vs route thật**  
Bằng chứng (path không trùng chữ; chỉ tương đương nghiệp vụ):

| DOC-12 / OAS | Code Host |
|--------------|-----------|
| `GET /lev/balances` | `GET /v1/lev/leave-balances/me` |
| `POST /lev/requests` | `POST /v1/lev/leave-requests` |
| `POST /lev/requests/{id}/c1` | `POST .../c1/approve` và `.../c1/reject` |
| `POST /lev/requests/{id}/c2` | `POST .../c2/approve` và `.../c2/reject` |
| `GET /prb/cases/{employeeId}` | `GET /v1/prb/cases` (list, không `{employeeId}`) |
| `POST /prb/cases/{id}/propose\|decide` | `POST /v1/prb/evaluations/{employeeId}/propose\|decide` |
| `GET /lif/cases/{employeeId}` | `GET /v1/lif/offboarding/{id}` / `onboarding/{id}` (caseId) |
| `POST /lif/cases/{employeeId}/locks` | `POST /v1/lif/offboarding/{id}/locks` |

Khớp chữ: `/iam/me`, `/emp/employees/{id}`, `/tim/imports`, `/tim/periods/{ym}/close`, `/pay/payslips/*`, `/pay/periods/{ym}/run`.  
Vì sao lỗi: client theo OAS sẽ 404; QC không viết contract test từ DOC-12.  
Đề xuất: SA cập nhật §4 + YAML theo controller **hoặc** đổi route (CR).

### 🟡 Major

**[Major] Mâu thuẫn — DOC-12 §2 vs DevAuth**  
`GET /dev/token` tồn tại; DEC-DLV-011 cho DEV/UAT; DOC-12 Chốt 2026-08-26 chưa ghi ngoại lệ.

**[Major] Testable — PK TBD**  
DOC-11 §1.2 / DOC-12 §4 vẫn “UUID hoặc long”. Code + route `{id:guid}` đã chốt Guid.

**[Major] Mâu thuẫn — DOC-11 §3.4 TIM**  
Giấy: 1 entity `TimesheetTemplate`. Code: `TimesheetTemplateVersion` + `Column` + import batch/row.

**[Major] Traceability — DOC-11 §8**  
Thiếu LEV (`LeaveBalance`/`LeaveRequest`) và TIM (`TimesheetPeriod`/`TimesheetLine`). INT-003/004 trỏ tên cũ (`ImportBatch`, `AccessLock`).

**[Major] Testable — NFR trống**  
DOC-11 §6 retention TBD; DOC-12 §7 Rate/Timeout TBD.

### ⚪ Minor

- DOC-11 §3.1 `AuthAudit` vs `EmpAuditLog` dùng chung.
- §3.2 `Contract`/`Education` vs `EmployeeContract`/`EducationLevel`.
- §3.8 Notification “nếu tách” vs `LeaveNotification` + `PayExportOutbox` cùng DB.
- §9 Business Owner ☐ Nợ vs DEC-DLV-008 Ban HR đã ký.

---

## Trace gaps

| Chuỗi | Trạng thái |
|---|---|
| NFR-002 → Payslip → DB-PAY | **Đứt** |
| LEV-FR → LeaveBalance/Request | **Đứt** (§8) |
| TIM-FR → TimesheetPeriod/Line | **Đứt** (§8) |
| PRB-FR-009 → `/prb/.../decide` | Path giấy ≠ `/prb/evaluations/.../decide` |
| INT-004/005 → AccessLock | Code: `LifAccessLockOutbox` |
| INT-003 → ImportBatch | Code: `TimesheetImportBatch` |
| OAS path LEV/PRB/LIF | **Đứt contract** |

---

## Verdict

⛔ **BLOCK baseline** DOC-11 và DOC-12 — 4 Blocker, 5 Major, 4 Minor.

Không mở `02-baseline/` cho hai DOC này. Trạng thái **Chốt** không khớp code `hrm/`. Owner: **SA** (sửa DOC hoặc CR) · PGD chốt hướng single-DB.
