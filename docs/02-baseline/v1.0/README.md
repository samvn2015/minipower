# Baseline v1.0 — READ ONLY

Snapshot tài liệu tại thời điểm sign-off baseline.

**Quy tắc:**
- Không sửa file trong folder này trực tiếp.
- Thay đổi sau baseline → CR (`06-changes/`) → sửa bản working → approve → tạo `v1.1/` nếu cần.

Xem `manifest.yaml` khi baseline được tạo.
